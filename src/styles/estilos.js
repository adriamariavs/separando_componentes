import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
      container: {
      flex: 1,
      textAlign: 'center',
      justifyContent: 'center',
      width: '100%',
      height: '100%',
    },
    
    image: {
        flex: 1,
        width: '100%',
        height: '100%',
        textAlign: 'center',
        alignItems: 'center',
        justifyContent: 'center',
      },
    
    terciaryContainer: {
     alignItems: 'center',
     justifyContent: 'center',
    }
  })